Run VENV:

py -m venv .venv

.\.venv\Scripts\activate

Update Migrations:

set FLASK_APP=app.py

flask db migrate

flask db upgrade


Currently looking up how to fix up the top code.
Asking Bing to make it so I can create threads, go to thread pages and comment, and just view all
the threads on the forum page. There are errors currently.